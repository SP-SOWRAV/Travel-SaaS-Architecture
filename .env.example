DB_USER=ota_user
DB_PASSWORD=ota_password
DB_NAME=ota_saas_dev
DB_PORT=5432

DATABASE_URL=postgresql://ota_user:ota_password@localhost:5432/ota_saas_dev

# Comma-separated list of allowed frontend origins (API_RULES §21 — never '*')
CORS_ORIGIN=http://localhost:3000
